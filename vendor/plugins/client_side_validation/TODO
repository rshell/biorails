
* Better JavaScript class/method documentation. 
* Unit tests for status change callbacks.
* rake task for updating /public/javascripts
* Only add a single new property (__validation) to validated elements
* Help
* Hooks for reinstalling validations after page has
  ajaxly been changed. remove, reinstall
* Indicate to callbacks exactly which validation failed.
* Attach error messages to validation functions; 
  split validators into { check: function(...) {...}, msg: ' ' }
* Helper method

  def validation_classes(object, property)
    return nil if object.nil?
    BoilerPlate::ActionViewExtensions.client_side_validations(object,
    property).join(' ')
  end

<div class="content_header organisation">
    <%= render :partial => 'shared/help_link' %>
    <h1 class='icon icon-service-large'>
    	<%= link_to( "#{l(:Item)} #{l(:in)} #{l(:Service_Queue)} [#{ @queue_item.queue.name}]", service_url( :action => 'show', :id => @queue_item.queue.id)) %>
    </h1>
    <br/>
    <br/>
</div>
<%=breadcrumb_list @queue_item.queue.folder %>

<% content_for :sidemenu do%>
   <%= render :partial => '/menu/admin_menu'%>
<% end %>

<h2> Backup Database </h2>
Completed backup on database content. See server directory <b>[<%="#{RAILS_ROOT}/db/backup/" %>]</b> for portable data files.
These are created in portable <a href='http://en.wikipedia.org/wiki/YAML'>YAML</a> format for long term storage and migration to other database server.
You may prefer native database backups for general operations
 


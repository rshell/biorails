<div class='box'>
  <strong class='icon icon-project-lab'><%=l(:label_labs)%></strong>
    <dl>
    <% for item in @project.children_of_type(8,5) %>
      <dt><%= link_to item.name, project_url( :action => 'show', :id => item) %>
        </dt>
      <dd><%= item.description %>
        </dd>
     <% end %>
    </dl>
  <hr/>
  <%= link_to_if allows?(:data,:create), l(:label_lab), project_url(:action => 'new', :type=>5), :class=>'icon icon-new'%>
</div>
